Since the hough circles was getting too complex to be done in video,
This app tries to detect hough circles after capturing an image instead.

Note, this is not a working implementation. 
This is only there as a reference, since the project aims at doing shot placement in video.